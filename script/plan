#!/bin/bash

set -e

debug_flag=""

while [[ $# -gt 0 ]]; do
  case "$1" in
  --debug)
    debug_flag="--debug"
    shift
    ;;
  *)
    break
    ;;
  esac
done

octodns-sync \
  $debug_flag \
  --config-file ./config/main.yaml
